steps:
    - label: cache
      plugins:
        - chef/cache#poc_initial_updates:
            cached_folders:
            - nate_test/
      agents:
        queue: default
      timeout_in_minutes: 2
      commands:
        - ls -al ./