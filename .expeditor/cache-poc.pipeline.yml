---
expeditor:
  cached_folders:
    - nate_test

steps:
    - label: cache
      expeditor:
        cached_folders:
          - "!nate_test"
          - /usr/bin
      agents:
        queue: default
      timeout_in_minutes: 2
      commands:
        - ls -al ./